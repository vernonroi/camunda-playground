spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/camunda
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    database:
      type: postgres
      schema-update: true